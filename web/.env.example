# Demo: mock login + fixture merge for contact (see data/demo-api-fixtures.json).
# On Vercel (NODE_ENV=production), set DEMO_MODE=true or mock auth stays disabled.
# DEMO_MODE=true
# NEXT_PUBLIC_DEMO_MODE=true

# Optional: forward validated contact JSON to a real endpoint when DEMO_MODE=false
# CONTACT_FORWARD_URL=https://example.com/api/inquiries
